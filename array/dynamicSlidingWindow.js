/**************************
 * Find the shortest subarray that is equal or greater than x=7
 * i/p---->[1,2,3,4,5,6]
 * o/p----->2 (length)
 *
 */

const shortestSubArray = (arr, x) => {
  return arr;
};

const arr = [1, 2, 3, 4, 5, 6];
const x = 7;
const result = shortestSubArray(arr, x);
console.log(result);
